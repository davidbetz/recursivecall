version: '3'
services:
  a:
    image: davidbetz/recursivecall:healthcheck-latest
    environment:
      - NAME=a
      - DEBUG=trace
    networks:
      - recursivecallnet

  b:
    image: davidbetz/recursivecall:healthcheck-latest
    environment:
      - NAME=b
      - DEBUG=trace
    networks:
      - recursivecallnet
      
  c:
    image: davidbetz/recursivecall:healthcheck-latest
    environment:
      - NAME=c
      - DEBUG=trace
    networks:
      - recursivecallnet
      
  d:
    image: davidbetz/recursivecall:healthcheck-latest
    environment:
      - NAME=d
      - DEBUG=trace
    networks:
      - recursivecallnet
      
  e:
    image: davidbetz/recursivecall:healthcheck-latest
    environment:
      - NAME=e
      - DEBUG=trace
    networks:
      - recursivecallnet
      
  f:
    image: davidbetz/recursivecall:healthcheck-latest
    environment:
      - NAME=f
      - DEBUG=trace
    networks:
      - recursivecallnet
      
  g:
    image: davidbetz/recursivecall:healthcheck-latest
    environment:
      - NAME=g
      - DEBUG=trace
    networks:
      - recursivecallnet
    ports:
      - "3000:3000"

networks:
  recursivecallnet:
    driver: bridge
    driver_opts:
      encrypted: ""

